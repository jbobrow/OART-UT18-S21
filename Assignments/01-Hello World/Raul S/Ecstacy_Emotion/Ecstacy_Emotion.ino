void setup() {
  // put your setup code here, to run once:
  setColor(makeColorRGB(255, 183, 82));
  setColorOnFace(makeColorRGB(246, 250, 12), 0);
  setColorOnFace(makeColorRGB(246, 250, 12), 2);
  setColorOnFace(makeColorRGB(246, 250, 12), 4);
}

void loop() {
  // put your main code here, to run repeatedly:

}
